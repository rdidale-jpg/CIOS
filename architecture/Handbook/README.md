# Handbook

To create this directory on GitHub:

1. Open the `architecture` directory in the repository.
2. Select **Add file** > **Create new file**.
3. Enter `Handbook/README.md` as the file name.
4. Add a short description or placeholder content.
5. Select **Commit changes** to create the new `Handbook` directory.
